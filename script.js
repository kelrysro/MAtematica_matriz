let tr = Number(prompt("Quantas colunas?"))
let td = Number(prompt("Quantas linhas?"))

const totalelementos = tr * td

for (let linha = 0;  linha < td.length; ++) {
    const element = Number(prompt(`linha ${td} inscira o valor dos numeros da coluna ${td}`))    
}
